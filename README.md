software-rasteriser
===================

A simple software rasteriser written in C

To build and run:

1) Create build directory - `mkdir build && cd build`
2) Build - `cmake .. && make`
3) Run - `../bin/sr`